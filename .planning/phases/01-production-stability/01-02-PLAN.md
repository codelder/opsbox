---
phase: 01-production-stability
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/logseek/tests/boundary_integration.rs
autonomous: true
requirements:
  - SAFE-03

must_haves:
  truths:
    - "test_mixed_encoding_search searches files with different encodings and asserts results contain expected text"
    - "test_malicious_orl_protection parses path traversal payloads and verifies they are blocked/sanitized"
    - "test_concurrent_search_boundary runs 10+ concurrent searches and verifies result integrity"
    - "test_permission_denied_scenarios accesses restricted paths and asserts proper error responses"
    - "test_large_file_boundary searches a large file and verifies correct results with controlled memory"
  artifacts:
    - path: "backend/logseek/tests/boundary_integration.rs"
      provides: "5 boundary integration tests with real assertions"
      min_lines: 200
  key_links:
    - from: "backend/logseek/tests/boundary_integration.rs"
      to: "backend/logseek/src/service/search.rs"
      via: "SearchExecutor or SearchProcessor for running searches"
      pattern: "SearchProcessor|SearchExecutor"
    - from: "backend/logseek/tests/boundary_integration.rs"
      to: "backend/opsbox-core/src/dfs/orl_parser.rs"
      via: "ORL parsing for malicious input validation"
      pattern: "parse_orl|OrlParser"
---

<objective>
Implement real assertions for 5 stub boundary integration tests in boundary_integration.rs

Purpose: The current tests create files and print success messages but have no actual assertions -- they provide zero confidence in boundary behavior. Each test must verify actual search results, security blocking, concurrency safety, error handling, and memory constraints.

Output: boundary_integration.rs with 5 tests containing real assert!() calls that verify: encoding correctness, path traversal blocking, concurrent result integrity, permission error handling, and large file search correctness
</objective>

<execution_context>
@/Users/wangyue/.claude/get-shit-done/workflows/execute-plan.md
@/Users/wangyue/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@backend/logseek/tests/boundary_integration.rs
@backend/logseek/src/service/search.rs
@backend/logseek/src/service/search_executor.rs
@backend/opsbox-core/src/dfs/orl_parser.rs
@backend/test-common/src/file_utils.rs

# Test infrastructure available (from test-common):
# - TestFileGenerator::new() - creates temp dir
# - generator.create_file(name, content) - creates test file
# - generator.create_large_file(name, mb) - creates large test file
# - generator.dir() - returns temp dir path
# Test assertion patterns (from RESEARCH.md):
# - Use SearchProcessor or router oneshot for integration tests
# - collect_ndjson_events() for NDJSON response parsing
# - tempfile for temp directories
# encoding_rs::GBK for GBK encoding tests
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement test_mixed_encoding_search with real search assertions</name>
  <files>backend/logseek/tests/boundary_integration.rs</files>
  <behavior>
    - Creates UTF-8 BOM, UTF-8 plain, and GBK encoded files
    - Runs search for "Test" pattern across all files
    - Asserts search returns results
    - Asserts results contain expected text from each file
  </behavior>
  <action>
    1. In the existing `test_mixed_encoding_search` function:
       - Keep the file creation code (UTF-8 BOM, UTF-8 plain files)
       - Fix the GBK file: use encoding_rs::GBK to actually encode Chinese text as GBK bytes, write raw bytes to file
       - Import necessary types: `use logseek::service::search::SearchProcessor;` (or the appropriate search API)
       - Create a SearchProcessor or use the search executor to search for a pattern like "Test" or "测试"
       - Collect results and assert:
         - At least one result found in UTF-8 files
         - Result content contains expected text
         - If GBK search is supported, assert GBK results found too
    2. Remove the println-only output, replace with assert!() calls
    3. If SearchProcessor API is complex, use the logseek query parser to create a Query and search individual files
  </action>
  <verify>
    <automated>cd /Users/wangyue/workspace/codelder/opsbox && OPSBOX_NO_PROXY=1 cargo test -p logseek --test boundary_integration test_mixed_encoding_search -- --nocapture 2>&1 | tail -20</automated>
  </verify>
  <done>Test searches files with different encodings, asserts results found, no TODO comments remaining</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement test_malicious_orl_protection with path traversal verification</name>
  <files>backend/logseek/tests/boundary_integration.rs</files>
  <behavior>
    - Parses each malicious ORL string
    - Verifies path traversal patterns are rejected or sanitized
    - Asserts no path escapes the allowed root directory
    - Tests null byte injection, command injection, and traversal payloads
  </behavior>
  <action>
    1. In the existing `test_malicious_orl_protection` function:
       - Import ORL parser: `use opsbox_core::dfs::orl_parser::parse_orl;` (check actual API)
       - For each malicious ORL in the vec:
         - Try to parse the ORL
         - If parsing succeeds, verify the resolved path does NOT escape the root
         - Assert path does not contain ".." after normalization
         - Assert path does not contain null bytes
         - Assert no command injection characters pass through to file system operations
       - For path traversal patterns specifically:
         - "orl://local/../../../etc/passwd" should resolve to a safe path or return error
         - Verify the resolved path starts with the expected root
    2. Replace the for-loop println with actual assertions
    3. Add assertions at the end confirming all patterns were handled safely
  </action>
  <verify>
    <automated>cd /Users/wangyue/workspace/codelder/opsbox && OPSBOX_NO_PROXY=1 cargo test -p logseek --test boundary_integration test_malicious_orl_protection -- --nocapture 2>&1 | tail -20</automated>
  </verify>
  <done>Test verifies all malicious ORL patterns are blocked or sanitized, no TODO comments</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Implement test_concurrent_search_boundary with 10+ parallel searches</name>
  <files>backend/logseek/tests/boundary_integration.rs</files>
  <behavior>
    - Creates 10+ test log files with known content
    - Spawns 10+ concurrent search tasks
    - Each task searches for "ERROR" pattern
    - Asserts all tasks complete without panic
    - Asserts each task returns expected results
    - Verifies no data corruption between concurrent searches
  </behavior>
  <action>
    1. In the existing `test_concurrent_search_boundary` function:
       - Keep the 10-file creation code
       - Replace the mock sleep tasks with actual search operations
       - Use tokio::spawn to run concurrent searches:
         - Each task searches one file for "ERROR"
         - Collect results via mpsc channel or JoinHandle
       - Assert:
         - All 10 tasks complete successfully (no panics)
         - Each file that contains "ERROR" returns at least one result
         - Results contain the expected file-specific content
    2. Remove the placeholder sleep and println, replace with real search + assertions
  </action>
  <verify>
    <automated>cd /Users/wangyue/workspace/codelder/opsbox && OPSBOX_NO_PROXY=1 cargo test -p logseek --test boundary_integration test_concurrent_search_boundary -- --nocapture 2>&1 | tail -20</automated>
  </verify>
  <done>Test runs 10+ concurrent searches, asserts all complete without panic, results are correct</done>
</task>

<task type="auto" tdd="true">
  <name>Task 4: Implement test_permission_denied_scenarios with real error assertions</name>
  <files>backend/logseek/tests/boundary_integration.rs</files>
  <behavior>
    - Creates a file with restricted permissions (Unix: chmod 000)
    - Attempts to search the restricted file
    - Asserts search returns appropriate error (not panic)
    - On Windows or CI: uses non-existent path to test error handling
  </behavior>
  <action>
    1. In the existing `test_permission_denied_scenarios` function:
       - Create a temp file with known content
       - On Unix: use std::fs::set_permissions to remove read permissions (mode 0o000)
       - Attempt to search the restricted file
       - Assert: search returns Err or empty results (NOT panic)
       - Assert: error message mentions permission or access denied
       - Add fallback for Windows/CI: test with non-existent path
    2. Remove the TODO and println, replace with assertions
    3. Clean up: restore permissions or let tempfile handle cleanup
  </action>
  <verify>
    <automated>cd /Users/wangyue/workspace/codelder/opsbox && OPSBOX_NO_PROXY=1 cargo test -p logseek --test boundary_integration test_permission_denied_scenarios -- --nocapture 2>&1 | tail -20</automated>
  </verify>
  <done>Test verifies search handles permission errors gracefully, returns error not panic</done>
</task>

<task type="auto" tdd="true">
  <name>Task 5: Implement test_large_file_boundary with search correctness verification</name>
  <files>backend/logseek/tests/boundary_integration.rs</files>
  <behavior>
    - Creates a 10MB file with known patterns at specific offsets
    - Searches for the patterns
    - Asserts all patterns are found
    - Asserts search completes in reasonable time
    - Verifies no out-of-memory issues
  </behavior>
  <action>
    1. In the existing `test_large_file_boundary` function:
       - Keep the 10MB file creation
       - Before searching, inject known patterns into the file:
         - Write "UNIQUE_MARKER_1" at the beginning
         - Write "UNIQUE_MARKER_2" in the middle (seek to 5MB)
         - Write "UNIQUE_MARKER_3" at the end
       - Search for each marker pattern
       - Assert: all 3 markers found
       - Assert: search completes within a timeout (e.g., 30 seconds)
       - Add timing measurement for performance baseline
    2. Remove the TODO and println, replace with assertions
  </action>
  <verify>
    <automated>cd /Users/wangyue/workspace/codelder/opsbox && OPSBOX_NO_PROXY=1 cargo test -p logseek --test boundary_integration test_large_file_boundary -- --nocapture 2>&1 | tail -20</automated>
  </verify>
  <done>Test searches 10MB file, finds all injected patterns, completes within timeout</done>
</task>

</tasks>

<verification>
1. Run all boundary tests: `OPSBOX_NO_PROXY=1 cargo test -p logseek --test boundary_integration`
2. Verify no TODO comments remain: grep for TODO in boundary_integration.rs
3. Verify all 5 tests have assert!() calls
</verification>

<success_criteria>
- All 5 boundary tests contain real assertions (no println-only tests)
- test_mixed_encoding_search: searches and verifies results across UTF-8/GBK files
- test_malicious_orl_protection: verifies path traversal is blocked
- test_concurrent_search_boundary: 10+ parallel searches complete without panic
- test_permission_denied_scenarios: handles permission errors gracefully
- test_large_file_boundary: searches 10MB file, finds injected markers
- All tests pass with `cargo test -p logseek --test boundary_integration`
</success_criteria>

<output>
After completion, create `.planning/phases/01-production-stability/01-02-SUMMARY.md`
</output>
