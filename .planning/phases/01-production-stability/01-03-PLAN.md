---
phase: 01-production-stability
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/logseek/tests/s3_integration.rs
autonomous: true
requirements:
  - SAFE-04

must_haves:
  truths:
    - "test_s3_api_endpoints creates test router, starts MockS3Server, sends HTTP requests"
    - "Profile CRUD operations verified through API responses (create, list, delete)"
    - "S3 search flow tested with mock server returning preset data"
    - "All assertions verify actual HTTP status codes and response bodies"
  artifacts:
    - path: "backend/logseek/tests/s3_integration.rs"
      provides: "S3 API endpoint tests with mock server and real assertions"
      min_lines: 150
  key_links:
    - from: "backend/logseek/tests/s3_integration.rs"
      to: "backend/logseek/src/routes.rs"
      via: "Test router creation for API endpoint testing"
      pattern: "create_router|Router"
    - from: "backend/logseek/tests/s3_integration.rs"
      to: "backend/test-common/src/s3_mock.rs"
      via: "MockS3Server for S3 API simulation"
      pattern: "MockS3Server|start_mock_s3_server"
    - from: "backend/logseek/tests/s3_integration.rs"
      to: "backend/logseek/src/repository/s3.rs"
      via: "S3 profile CRUD operations"
      pattern: "s3_repo::"
---

<objective>
Implement test_s3_api_endpoints with MockS3Server and real HTTP request assertions

Purpose: The current test is a placeholder that only prints a skip message. It must test the S3 profile management API (create/list/delete) and S3 search flow using the existing MockS3Server infrastructure from test-common.

Output: s3_integration.rs with test_s3_api_endpoints that creates a test router, starts MockS3Server, sends HTTP requests through axum test utilities, and asserts correct responses
</objective>

<execution_context>
@/Users/wangyue/.claude/get-shit-done/workflows/execute-plan.md
@/Users/wangyue/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@backend/logseek/tests/s3_integration.rs
@backend/logseek/src/routes.rs
@backend/logseek/src/repository/s3.rs
@backend/test-common/src/s3_mock.rs
@backend/test-common/src/database.rs

# Existing test infrastructure:
# - test_s3_settings_crud: Already works, tests repo-level CRUD
# - test_s3_connection_test: Tests mock server + profile save
# MockS3Server API:
#   - start_mock_s3_server(port) -> Result<MockS3Server>
#   - server.endpoint() -> String (base URL)
#   - server.stop().await
# TestDatabase:
#   - TestDatabase::in_memory() -> Result<TestDatabase>
#   - init_logseek_schema(&db.pool).await
# LogSeek router creation pattern:
#   - Need to create axum Router with logseek routes for testing
#   - Use tower::ServiceExt::oneshot() for request testing
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement test_s3_api_endpoints with MockS3Server and HTTP assertions</name>
  <files>backend/logseek/tests/s3_integration.rs</files>
  <behavior>
    - Starts MockS3Server on unique port
    - Creates test database with logseek schema
    - Creates test router with logseek routes
    - Tests POST /api/v1/logseek/profiles to create profile
    - Tests GET /api/v1/logseek/profiles to list profiles
    - Tests DELETE /api/v1/logseek/profiles/{name} to delete profile
    - All tests assert HTTP status codes and response bodies
    - Cleans up mock server
  </behavior>
  <action>
    1. In the existing `test_s3_api_endpoints` function:
       a. Start MockS3Server:
          - Use a unique port (e.g., opsbox_test_common::constants::S3_PORT_START + 10)
          - Handle port conflict gracefully (skip if unavailable)
       b. Create test infrastructure:
          - `let db = TestDatabase::in_memory().await.expect("...");`
          - `init_logseek_schema(&db.pool).await.expect("...");`
       c. Create test router:
          - Check how logseek creates its router in src/routes.rs or src/api.rs
          - Use the same pattern to create a test router with the database pool
          - May need to create AppState or similar test state
       d. Test profile creation:
          - Build POST request to /api/v1/logseek/profiles
          - Body: S3Profile with mock server endpoint
          - Assert: 201 Created or 200 OK
          - Assert: response contains created profile
       e. Test profile listing:
          - Build GET request to /api/v1/logseek/profiles
          - Assert: 200 OK
          - Assert: response contains the created profile
       f. Test profile deletion:
          - Build DELETE request to /api/v1/logseek/profiles/test-profile
          - Assert: 200 OK or 204 No Content
          - Verify deletion with another GET (should be empty or 404)
       g. Clean up: `mock.stop().await.expect("...");`
    2. Import necessary types:
       - use axum::body::Body;
       - use http::{Request, StatusCode};
       - use tower::ServiceExt; (for oneshot)
       - S3Profile from opsbox_core::repository::s3
    3. Replace the TODO placeholder with real test code
  </action>
  <verify>
    <automated>cd /Users/wangyue/workspace/codelder/opsbox && OPSBOX_NO_PROXY=1 cargo test -p logseek --test s3_integration test_s3_api_endpoints -- --nocapture 2>&1 | tail -30</automated>
  </verify>
  <done>test_s3_api_endpoints tests profile CRUD through HTTP API, all assertions pass</done>
</task>

</tasks>

<verification>
1. Run S3 integration tests: `OPSBOX_NO_PROXY=1 cargo test -p logseek --test s3_integration`
2. Verify test_s3_api_endpoints has real assertions (not just println)
3. Verify mock server is properly started and stopped
</verification>

<success_criteria>
- test_s3_api_endpoints creates and uses a test router
- MockS3Server starts successfully (or test skips gracefully on port conflict)
- Profile creation tested via HTTP POST with status assertion
- Profile listing tested via HTTP GET with response body assertion
- Profile deletion tested via HTTP DELETE with verification
- All assertions check actual HTTP status codes and response content
- Existing tests (test_s3_settings_crud, test_s3_connection_test) still pass
</success_criteria>

<output>
After completion, create `.planning/phases/01-production-stability/01-03-SUMMARY.md`
</output>
